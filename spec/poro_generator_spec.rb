require "spec_helper"

describe PoroGenerator do
  it "has a version number" do
    expect(PoroGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
